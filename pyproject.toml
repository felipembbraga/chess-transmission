[project]
name = "chess-transmission"
version = "1.0.0"
description = "Recognizes chess moves played on a physical board via camera and transmits them live to a Lichess Broadcast round"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Felipe Mendes Braga", email = "felipe.mendes.braga@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "berserk>=0.14.0",
    "numpy>=2.5.1",
    "opencv-python>=5.0.0.93",
    "python-chess>=1.999",
    "python-dotenv>=1.2.2",
]

[project.scripts]
chess-transmission = "chess_transmission.cli.main:main"

[build-system]
requires = ["uv_build>=0.9.27,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "black>=26.5.1",
    "pytest>=9.1.1",
    "pytest-mock>=3.15.1",
    "responses>=0.26.2",
]

[tool.black]
target-version = ["py312"]
